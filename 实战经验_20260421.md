# AI客服系统实战问题与解决方案

> 记录时间：2026-04-21
> 项目：ai-customer-service（共享单车AI客服）

---

## 一、向量检索升级踩坑

### 问题背景
想给知识库添加向量检索（语义匹配），替代现有的纯关键词匹配。

### 踩坑过程

#### 1. DeepSeek Embedding 不可用
**尝试方案**：使用 DeepSeek 自带的 Embedding API

**结果**：404 错误，API 不存在

**原因**：DeepSeek 官方明确回复"目前不支持 Embedding 和 Function Calling"

**教训**：不是所有大模型 API 都支持 Embedding，动手前先查官方文档

---

#### 2. 本地 sentence-transformers 安装失败
**尝试方案**：安装 text2vec / sentence-transformers 本地模型

**结果**：
- 清华源 403 错误
- 默认源下载太慢（首次下载模型约 400MB）

**教训**：国内网络环境下，大模型依赖安装需要提前测试速度，或准备镜像源

---

### 最终决定
放弃向量检索方案，保留纯关键词匹配，原因：
1. 当前知识库规模不大（13条），关键词匹配够用
2. 增加复杂度带来的收益不明显
3. DeepSeek Embedding 不可用，本地模型部署成本高

---

### 后续如果要做向量检索，推荐方案
| 方案 | 优点 | 缺点 |
|------|------|------|
| 智谱 Embedding API | 国内直连，效果好 | 需要申请 API Key |
| 阿里云 DashScope | 免费额度够用 | 需要注册账号 |
| HuggingFace 在线 | 免费 | 需要网络访问 |

---

## 二、管理后台 404 问题

### 问题现象
打开管理后台 → 知识库管理页面 → 显示 "知识库接口404"

### 排查过程
1. 用 curl 测试 API：`curl http://localhost:3456/api/knowledge` → 正常返回 200
2. 确认后端路由没问题
3. 检查前端代码发现调用路径不一致

### 根本原因
**前端和后端 API 路径不匹配**

| 位置 | 前端调用 | 后端路由 |
|------|---------|---------|
| loadKbSummary() | `/knowledge/list` | `/knowledge` |
| loadKnowledge() | `/knowledge/list` | `/knowledge` |

**教训**：后端改了路由，前端没同步更新

---

### 修复方案
```
// 修复前
const res = await apiFetch(API + '/knowledge/list');
const total = data.items.length;

// 修复后
const res = await apiFetch(API + '/knowledge');
const total = data.data.length;
```

---

### 经验总结
1. **前后端 API 路径必须同步维护**
   - 建议：建立 API 文档或常量定义，避免散落各处

2. **接口返回格式也要注意**
   - `data.items` vs `data.data`
   - 后端改了返回结构，前端也要跟着改

---

## 三、代码回滚经验

### 需要回滚的文件清单
| 文件 | 操作 |
|------|------|
| `server/services/embedding.js` | 删除 |
| `test-embedding.js` | 删除 |
| `server/services/knowledge.js` | 恢复原版，移除向量方法 |
| `server/routes/knowledge.js` | 恢复原版，移除向量 API |
| `server/services/ai.js` | 恢复 chat() 方法为同步调用 |

### 回滚后必须验证
- [x] 服务启动正常
- [x] curl 测试 API 返回正确数据
- [x] 管理后台页面加载正常

---

## 四、通用经验

### 1. 动手前先验证
- 第三方 API 先用 curl/test 脚本验证是否可用
- 不要假设 API 存在或格式正确

### 2. 改代码前先备份
- 可以保留一个 `embedding.js.bak` 作为参考
- 方便出问题快速回滚

### 3. 渐进式升级
- 新功能用独立文件引入，不要直接修改核心文件
- 降低回滚成本

### 4. 单元测试 + 集成测试
- API 改了路由，先用 curl 测通再调前端
- 前端改完要刷新页面验证

---

## 五、后续优化方向

### 知识库检索优化（可选）
1. **短期**：优化关键词提取算法，加同义词库
2. **中期**：接入智谱 Embedding（如果申请到 API Key）
3. **长期**：迁移到带向量检索能力的数据库（如 PostgreSQL + pgvector）

### 知识库 API 规范（建议）
```javascript
// 统一 API 路径
GET    /api/knowledge        // 获取列表
GET    /api/knowledge/:id    // 获取单个
POST   /api/knowledge        // 新增
PUT    /api/knowledge/:id    // 更新
DELETE /api/knowledge/:id    // 删除
POST   /api/knowledge/batch  // 批量导入

// 统一响应格式
{
  success: true,
  data: [...],     // 数据列表
  total: 13        // 总数
}
```
