# PRD - AI 客服知识库管理模块 V2.0

**文档版本**：V1.0
**撰写人**：[你的名字]
**撰写时间**：2026-04-15
**状态**：已上线

---

## 一、背景与目标

### 1.1 业务背景

当前知识库管理存在以下问题：
- 录入效率低：运营人员每次只能单条添加，50 条知识需 2 小时
- 维护成本高：修改答案后需同步更新所有相关条目
- 批量导入能力缺失：无法复用历史 FAQ 数据

### 1.2 需求来源

运营同事反馈（2026-04-10）

### 1.3 目标

| 指标 | 当前值 | 目标值 |
|------|--------|--------|
| 知识库录入效率 | 2分钟/条 | 30秒/条 |
| 批量导入成功率 | — | ≥95% |
| 上线后 30 天知识库覆盖率 | 18 条 | ≥80 条 |

---

## 二、功能需求

### 2.1 功能列表

| 功能点 | 优先级 | 描述 |
|--------|--------|------|
| Excel 批量导入 | P0 | 支持 xlsx/csv 格式，含字段映射 |
| Excel 批量导出 | P0 | 导出全量知识库供运营二次编辑 |
| CSV 导入/导出 | P1 | 满足技术能力较弱用户的协作需求 |
| 重复条目检测 | P1 | 导入时提示相似问题，避免重复 |

### 2.2 用户故事

**故事 1：运营人员批量导入 FAQ**
> 作为运营人员，我希望能够批量导入 Excel 格式的 FAQ，这样就不用逐条添加，节省 80% 的录入时间。

验收标准：
- 上传 xlsx 文件后，系统自动识别"问题""答案""关键词"三列
- 导入结果实时展示：成功 X 条，跳过 Y 条
- 跳过条目显示原因（重复 / 字段缺失）

**故事 2：运营人员导出知识库**
> 作为运营人员，我希望能够导出知识库到 Excel，这样可以在本地用 Excel 编辑后再批量更新。

验收标准：
- 导出文件包含：问题、答案、关键词、创建时间
- 导出文件名格式：`知识库导出_YYYYMMDD.xlsx`

---

## 三、非功能需求

| 需求 | 说明 |
|------|------|
| 性能 | 导入 500 条数据，响应时间 ≤ 5 秒 |
| 容错 | 导入文件损坏时，提示具体错误行 |
| 兼容性 | 支持 Excel 97-2003（.xls）及 Excel 2007+（.xlsx） |

---

## 四、数据格式规范

### 4.1 Excel 模板字段

| 列名 | 必填 | 说明 | 示例 |
|------|------|------|------|
| 问题 | ✅ | 访客可能提问的方式 | 怎么联系人工？ |
| 答案 | ✅ | 标准回复内容 | 电话：400-xxx-xxxx |
| 关键词 | ❌ | 逗号分隔的同义词 | 人工,客服,联系 |

### 4.2 字段名兼容

系统支持以下字段名自动识别（不区分大小写）：

- 问题：问题 / question / Q
- 答案：答案 / answer / A
- 关键词：关键词 / keywords / 关键字

---

## 五、交互设计

### 5.1 批量导入流程

```
用户点击"批量导入"
       ↓
  选择文件（xlsx/csv）
       ↓
  系统解析 + 字段映射
       ↓
  显示预览（成功/跳过/失败）
       ↓
  用户确认导入
       ↓
  返回结果
```

### 5.2 错误提示

| 错误类型 | 提示文案 |
|----------|---------|
| 文件格式不对 | 请上传 .xlsx、.xls 或 .csv 格式文件 |
| 文件损坏 | 文件无法读取，请重新导出后上传 |
| 字段缺失 | 第 3 行"问题"字段为空，已跳过 |

---

## 六、技术方案

| 决策点 | 方案 | 理由 |
|--------|------|------|
| 解析库 | xlsx（SheetJS） | 前端可直接解析，无需上传服务器 |
| 重复检测 | 问题文本相似度 > 90% 视为重复 | 避免完全重复，允许小版本差异 |
| 导入模式 | 幂等导入（重复则更新） | 便于运营迭代修改 |

---

## 七、埋点需求

| 事件 | 触发时机 | 上报字段 |
|------|----------|---------|
| batch_import | 点击确认导入 | success_count, skip_count, file_size |
| batch_export | 点击导出 | format, item_count |
| import_error | 导入失败 | error_type, row_number |

---

## 八、测试用例摘要

| 用例 | 步骤 | 预期结果 |
|------|------|---------|
| 正常导入 | 上传含 5 条的 xlsx | 成功 5 条，跳过 0 条 |
| 重复导入 | 再次上传相同文件 | 成功 0 条，跳过 5 条 |
| 格式错误 | 上传损坏的 xlsx | 提示文件损坏 |
| 字段缺失 | 上传缺少答案列的 xlsx | 提示第 X 行答案为空 |

---

## 九、上线标准

- [ ] 测试用例全部通过
- [ ] 运营人员完成 UAT（用户验收测试）
- [ ] 上线后 7 天内知识库覆盖率 ≥ 50 条
- [ ] 无线上客诉（知识库相关）

---

*评审记录：2026-04-16 评审通过，产品/开发/测试三方签字*
