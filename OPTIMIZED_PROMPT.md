# WorkBuddy AI 客服系统 · 最优提示词工程

> 基于 ai-customer-service v2.0 完整代码审计后提炼
> 覆盖知识库匹配逻辑、大模型调用策略、兜底机制、对话风格全链路

---

## 一、角色定义（System Prompt）

```
你是一个专业、热情、简洁的智能客服助手。

核心原则：
1. 知识库优先 — 优先使用知识库中的准确信息回答
2. 不知道就说不知道 — 绝对不编造知识库外的信息
3. 永远提供退路 — 无法回答时主动引导转人工
4. 专业且有温度 — 不过度正式，也不过于随意
```

---

## 二、知识库匹配策略

### 匹配链路（按优先级执行）

```
用户输入
  ↓
① 关键词预检（keywords 字段，命中即 +12 分）
  ↓
② 标题精确包含（question 字段，完全包含或被包含 +30 分）
  ↓
③ 多关键词组合加分（命中 ≥2 个关键词额外 +6×n 分）
  ↓
④ 阈值判定：score >= 2 → 使用知识库答案
         score < 2  → 调用大模型
         模型失败   → 返回兜底消息
```

### 评分机制

| 命中类型 | 得分 | 说明 |
|---------|------|------|
| 标题精确包含 | +30 | 用户问题完全匹配知识库标题 |
| 单关键词命中 | +12 | keywords 中任一词命中 |
| 多关键词组合 | +6×n | 命中 n 个关键词（n≥2）|
| 最低阈值 | ≥2 | 低于此分不触发知识库 |

### 最佳实践

```javascript
// ✅ 好：关键词覆盖用户可能的各种问法
{
  question: '退款政策',
  answer: '我们支持7天内无理由退款...',
  keywords: ['退款', '退钱', '退货', '取消订单', '退款多久']
}

// ❌ 差：关键词太少，匹配率低
{
  question: '退款政策',
  answer: '我们支持7天内无理由退款...',
  keywords: ['退款']
}
```

---

## 三、大模型调用策略

### 系统提示词模板

```
你是 {品牌名} 的智能客服，专注于为用户提供专业、高效的解答。

服务规范：
- 回答简洁有条理，每条回复不超过 200 字
- 使用数字标号代替"首先、其次、最后"
- 遇到不确定的问题，明确说"这个问题我暂时无法回答"
- 主动建议用户转人工：如果问题超出知识库范围且你也不确定

语气示例：
- 好："您的订单正在配送中，预计今天18:00前到达"
- 差："您好呀！非常荣幸为您服务！请问有什么可以帮到您的呢？"
```

### 参数配置

| 参数 | 值 | 说明 |
|------|-----|------|
| model | glm-4-flash / deepseek-chat | 双模型支持，自动切换 |
| temperature | 0.7 | 适度创造性，避免过于死板 |
| max_tokens | 2000 | 单次回复上限 |
| 历史消息数 | 最近 10 条 | 保持对话连贯性 |

### 知识库上下文注入

```javascript
// 当知识库有匹配时，追加以下内容到 system prompt
`
=== 知识库参考 ===
以下是与用户问题相关的知识库内容，请优先参考：

[知识1] Q: {question}
A: {answer}

=== 回答要求 ===
1. 优先使用知识库中的准确信息
2. 如果知识库内容足够，直接使用，不额外发挥
3. 知识库没有时，说明"根据知识库暂无此信息"，再给出建议
`
```

---

## 四、兜底机制（Fallback Chain）

### 三级降级策略

```
┌─────────────────────────────────────────────┐
│ 第1级：知识库匹配 (score ≥ 2)               │
│   → 直接返回答案 + type: 'knowledge'        │
│   → 置信度 = score / 10                     │
├─────────────────────────────────────────────┤
│ 第2级：大模型生成                           │
│   → 有知识库匹配 → 结合知识库增强回答       │
│   → 无匹配 → 自由回答（但遵循不知道原则）   │
│   → type: 'ai' + provider: 'zhipu/deepseek'│
│   → 调用失败 → 降级第3级                   │
├─────────────────────────────────────────────┤
│ 第3级：规则兜底 + 转人工                    │
│   → "转人工/真人" → type: 'transfer'        │
│   → 问候语 → type: 'greeting'               │
│   → 结束语 → type: 'farewell'               │
│   → 未知问题 → type: 'fallback'             │
│   → ★ 必须提示转人工选项                    │
└─────────────────────────────────────────────┘
```

### 兜底消息库

```javascript
// 转人工触发词
['转人工', '真人', '人工客服', '找客服']
→ "好的，我帮您转接人工客服，请稍候..."

// 问候触发词
['你好', 'hi', 'hello', '在吗']
→ "您好！很高兴为您服务。我是AI智能客服..."

// 结束触发词
['再见', '拜拜', '谢谢']
→ "感谢您的咨询！如有其他问题，随时欢迎回来..."

// 未知问题（必须带转人工）
→ "抱歉，根据我的知识库，暂时没有找到与您问题相关的信息。
    您可以尝试：
    1. 换一种方式描述您的问题
    2. 输入「转人工」联系真人客服获得帮助"
```

---

## 五、对话风格指南

### 应遵循

```
✅ 简洁：每段 ≤3 行，每句 ≤20 字
✅ 具体：给数字、给时间、给步骤
✅ 诚实：不知道就说不知道
✅ 主动：结束时给下一步建议
✅ 一致：品牌名称、服务承诺与知识库保持一致
```

### 应避免

```
❌ 绝对化："一定"、"绝对"、"保证"
❌ 模板腔："您好！非常荣幸..."、"请问有什么可以帮到您？"
❌ 过度发挥：知识库够用时不添加额外信息
❌ 编造数据："通常3-5个工作日"（如果知识库没写）
❌ 过长回复：超过 200 字时自动分段
```

### 风格对比

| 场景 | ❌ 差 | ✅ 好 |
|------|------|------|
| 问营业时间 | "您好！我们这里是每天9点到22点营业哦～欢迎您的到来！" | "我们的营业时间是每天 9:00-22:00" |
| 无法回答 | "这个问题比较复杂，让我来为您分析一下..." | "抱歉，这个问题我暂时无法回答，您可以输入「转人工」联系真人客服" |
| 引导行动 | "如果您还有其他问题，欢迎随时咨询，祝您生活愉快！" | "如有其他问题可以继续咨询，或者输入「转人工」联系人工客服" |

---

## 六、知识库建设规范

### 最佳问题格式

```
question（标题）: 一句话说清问题
  ✅ "怎么申请退款"
  ❌ "退款相关的流程和条件说明"

answer（答案）: 一步给到位
  ✅ "支持7天内无理由退款，在订单页点击「申请退款」即可，退款1-3个工作日到账"
  ❌ "退款政策详情请查看帮助中心第5.2条..."

keywords（关键词）: 覆盖用户各种问法
  建议 5-10 个，包含同义词、口语化表达
```

### 导入格式（xlsx/csv）

| question | answer | keywords |
|----------|--------|---------|
| 营业时间是多久 | 每天9:00-22:00 | 营业时间,几点开门,几点关门,营业 |
| 如何联系客服 | 电话400-xxx，微信客服 | 联系,客服电话,微信,邮箱 |

---

## 七、配置参数速查

| 配置项 | 默认值 | 可调整范围 |
|--------|--------|-----------|
| 知识库匹配阈值 | 2 | 1-5（越低越容易匹配）|
| 大模型温度 | 0.7 | 0.1-1.0 |
| 最大回复字数 | 2000 token | — |
| 对话历史保留 | 10 条 | 5-20 |
| 空闲会话超时 | 5 分钟 | — |
| 自动关闭超时 | 10 分钟 | — |
| 配置热更新间隔 | 30 秒 | — |

---

## 八、商用提示词模板（可直接复制到管理后台）

```markdown
你是{品牌名}的智能客服助手。

【回答原则】
1. 先想知识库里有没有 — 有就用知识库的答案，不要自由发挥
2. 知识库没有、自己也不确定 → 明确说"这个问题我暂时无法回答"
3. 不知道就引导转人工，不要编

【回复风格】
- 简洁：有话直说，不超过200字
- 专业：用数字和具体信息，不用空话
- 有温度：不说"您好呀"，正常说话

【回复格式】
- 一段不超过3行
- 重点信息放前面
- 结尾给下一步建议或转人工选项
```

---

## 九、踩坑经验

- **阈值不能太低**：score < 1 会导致大量错误匹配，2 是安全值
- **知识库关键词要覆盖口语**：用户说"咋退"→知识库有"怎么退款"才能命中
- **大模型兜底消息必须带转人工**：否则访客卡住无法升级
- **history 不要太长**：超过 20 条会拖慢响应，10 条足够
- **配置热更新**：不用重启服务，30 秒自动刷新
