# AI客服系统 (ai-customer-service) 实战问题与解决方案

> 记录时间：2026-04-21
> 项目路径：`D:\AI应用\workbuddy files\ai-customer-service`
> 技术栈：Express + WebSocket + SQLite

---

## 一、核心 Bug 修复（2026-04-21）

### 1.1 访客端无法获取 AI 回复

**问题现象**
- 访客提问后显示"抱歉，我无法回答这个问题"
- AI 配置正常但就是不回答

**排查过程**
1. 检查 AI 服务调用链路
2. 检查知识库匹配结果
3. 检查敏感词过滤逻辑
4. 逐步定位到三个问题点

**根本原因（三个叠加）**

| 问题点 | 具体表现 | 原因 |
|--------|---------|------|
| 知识库阈值过高 | 匹配分数 >= 4 才返回，知识库命中失效 | score >= 4 太严格，常见问题只有 2-3 分 |
| 兜底消息过期 | 知识库+AI都失败时，兜底消息写的是旧逻辑 | 代码没同步更新 |
| 敏感词误杀 | "扫码"、"二维码" 等业务词被过滤 | 敏感词库包含业务关键词 |

**修复方案**

```javascript
// 1. 降低知识库阈值
// 修复前：score >= 4
// 修复后：score >= 2

// 2. 更新兜底消息
const fallbackMessage = "抱歉，这个问题我暂时无法回答。请您拨打客服热线 400-XXX-XXXX 转人工咨询。";

// 3. 移除业务关键词
// 从 sensitive-words.json 中删除 "扫码"、"二维码" 等
```

**涉及文件**
- `server/services/knowledge.js` - 降低阈值
- `server/routes/chat.js` - 更新兜底消息 + 改进AI服务逻辑
- `server/services/ai.js` - 优化 chat() 方法的知识库优先逻辑
- `server/data/sensitive-words.json` - 移除业务关键词

---

## 二、Windows 服务启动问题

**问题现象**
- 使用 PowerShell Start-Process 启动服务时，node 进程的工作目录不正确
- 服务启动但文件读取路径出错

**原因**
Start-Process 的 `-WorkingDirectory` 参数对 node 子进程可能不生效

**解决方案**
使用 `cmd /c` 启动批处理脚本，确保从正确目录运行：

```batch
@echo off
taskkill /F /IM node.exe 2>nul
cd /d "%~dp0"
start cmd /c "cd /d %~dp0 && npm start"
```

**关键点**
- `cd /d` 同时切换盘符和目录
- `start cmd /c` 在新窗口执行，确保目录正确

---

## 三、向量检索升级踩坑（已回滚）

### 3.1 DeepSeek Embedding API 不存在

**尝试方案**
使用 DeepSeek 自带的 Embedding API

**结果**
HTTP 404 - 端点不存在

**原因**
DeepSeek 官方明确回复：目前不支持 Embedding 和 Function Calling

**教训**
不是所有大模型 API 都支持 Embedding，动手前先查官方文档

---

### 3.2 本地 sentence-transformers 安装失败

**尝试方案**
安装 text2vec / sentence-transformers 本地模型

**结果**
- 清华源 403 错误
- 默认源下载太慢（首次下载模型约 400MB+）

**教训**
国内网络环境下，大模型依赖安装需要提前测试速度

---

### 3.3 最终决定

放弃向量检索方案，保留纯关键词匹配：

| 考量 | 结论 |
|------|------|
| 当前知识库规模 | 13条，关键词匹配够用 |
| 复杂度收益比 | 增加向量检索带来的收益不明显 |
| 部署成本 | DeepSeek 不可用，本地模型部署麻烦 |

---

## 四、管理后台 404 问题

**问题现象**
打开管理后台 → 知识库管理页面 → 显示 "知识库接口404"

**排查过程**
1. curl 测试 API → 后端正常返回 200
2. 检查前端代码发现调用路径不一致

**根本原因**
前端和后端 API 路径不匹配

| 位置 | 前端调用 | 后端路由 |
|------|---------|---------|
| `loadKbSummary()` | `/knowledge/list` | `/knowledge` |
| `loadKnowledge()` | `/knowledge/list` | `/knowledge` |

**教训**
后端改了路由，前端代码没有同步更新

**修复方案**

```javascript
// 修复前
const res = await apiFetch(API + '/knowledge/list');
const total = data.items.length;

// 修复后
const res = await apiFetch(API + '/knowledge');
const total = data.data.length;
```

**涉及文件**
- `public/admin.html` - 两处 API 路径修改

---

## 五、项目经验总结

### 5.1 技术决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 知识库存储 | SQLite | 数据量可控，迁移成本低 |
| AI 提供商 | DeepSeek + 智谱双备 | 避免单点故障 |
| 检索方式 | 关键词匹配 | 当前规模够用，暂不上向量 |
| 敏感词过滤 | 本地词库 + AI生成双重检测 | 保证回复安全性 |

### 5.2 调试经验

| 场景 | 经验 |
|------|------|
| Windows 服务启动 | 用 `cmd /c` 启动批处理，确保目录正确 |
| API 调试 | 先用 curl 测试后端，确认通了再调前端 |
| 敏感词误杀 | 业务关键词要从敏感词库中排除 |
| 回滚操作 | 渐进式升级，用独立文件引入，不改核心文件 |

### 5.3 架构亮点（面试可讲）

1. **双层兜底机制**
   - 知识库匹配 → AI生成 → 敏感词检测 → 预设回复
   - 四层保障确保用户永远不会收到空消息

2. **双 AI 提供商切换**
   - DeepSeek + 智谱自动切换
   - 一个挂了自动切另一个

3. **知识库优先策略**
   - 知识库答案可控、可审计、可追溯
   - AI 生成泛化能力强但不可控，所以知识库优先

---

## 六、后续优化方向

### 短期（可快速实现）
- [ ] 优化关键词提取算法（加同义词库）
- [ ] 完善管理后台操作日志
- [ ] 添加数据导出功能

### 中期（需要规划）
- [ ] 接入智谱 Embedding（如果申请到 API Key）
- [ ] 添加多租户支持
- [ ] 实现 SLA 告警机制

### 长期（需要重构）
- [ ] 迁移到 PostgreSQL + pgvector（向量检索）
- [ ] 添加 RBAC 权限系统
- [ ] 实现 Webhook 集成

---

## 七、快速排查指南

### 服务启动失败
```bash
# 检查端口占用
netstat -ano | findstr :3456

# 杀掉进程
taskkill /F /PID <PID>

# 重启服务
cmd /c "D:\AI应用\workbuddy files\ai-customer-service\start.bat"
```

### 知识库不工作
```bash
# 测试 API
curl http://localhost:3456/api/knowledge

# 检查返回格式
# 应该返回 { success: true, data: [...], total: N }
```

### 访客端无法回答
1. 检查管理后台 → AI配置 是否启用
2. 检查知识库是否有匹配的条目
3. 检查敏感词库是否误杀了业务词
4. 查看服务端日志的 [AI服务] 输出

---

**文档维护**：每次修复问题后更新此文档
